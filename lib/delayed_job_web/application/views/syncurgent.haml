%h1
  API
%p.sub
  The list below contains Sync URGENT jobs (scheduled, or running)
%p.sub= "Showing #{start} to #{start + per_page} of #{@all_jobs.count} Sync URGENT jobs."
%ul.job
  - @jobs.each do |job|
    = partial :job, {:job => job}
= partial :next_more, :start => start, :total_size => @all_jobs.count, :per_page => per_page
